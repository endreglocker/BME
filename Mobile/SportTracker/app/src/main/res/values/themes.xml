<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SportTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryVariant">@color/standard</item>
        <item name="colorPrimary">@color/standard</item>
        <item name="android:navigationBarColor">@color/standard</item>
        <!--<item name="android:windowBackground">@color/test</item>-->
    </style>

    <style name="Theme.SportTracker" parent="Base.Theme.SportTracker" />

    <style name="Theme.SportTracker.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/standard</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/standard</item>
        <item name="postSplashScreenTheme">@style/Theme.SportTracker</item>
    </style>
</resources>